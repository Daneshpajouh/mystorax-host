---
name: mystorax-surfaces
description: List MystoraX capability surfaces (wired / refused / inventory).
---

# /mystorax-surfaces

Call `mystorax_surfaces` or `mystorax_capability_lookup` with $ARGUMENTS. Never claim a refused surface works.
