# MystoraX host credentials (example — never commit real values)

MYSTORAX_CONDUCTOR_URL=https://mx.parallex.ca
MYSTORAX_HOST_TOKEN=

# Optional: only if calling axiom tools through Conductor from a front that holds the token.
# Prefer Conductor-side token injection instead.
# MYSTORAX_AXIOM_MCP_TOKEN=

# Bio / Axiom MCP token stays on Conductor only (not required on peer fronts).
