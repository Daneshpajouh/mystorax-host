# Claude Science

1. Import `Daneshpajouh/mystorax-host`
2. `MYSTORAX_HOST_TOKEN` = host ingress token · URL `https://mx.parallex.ca`
3. Prefer `job_class=research` for literature/brainstorm (bridges). Keep Science turns for ledger + measurement.

Smoke: `curl -fsS -A Claude-User https://mx.parallex.ca/health`
