# Codex

```bash
cd mystorax-host
./setup.sh --front codex
# restart Codex; confirm: codex mcp get mystorax-conductor
./mx "your question"
```

Codex as a thin Hand still only applies artifacts. Heavy research: `./mx "…"` (bridges).
